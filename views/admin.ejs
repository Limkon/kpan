<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理員面板 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>管理員面板 - 用戶管理</h1>
            <nav>
                <a href="/files">返回文件列表</a>
                <a href="/logout">登出</a>
            </nav>
        </header>

        <% if (locals.message) { %>
             <p class="message <%= (locals.messageType === 'error' || (locals.message && (message.includes('失敗') || message.includes('错误') || message.includes('不能') || message.includes('無效的用戶ID')))) ? 'error-message' : '' %>"><%= message %></p>
        <% } %>

        <h2>用戶列表</h2>
        <% if (users.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>用戶名</th>
                        <th>角色</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                        <tr>
                            <td><%= u.id %></td>
                            <td><%= u.username %></td>
                            <td><%= u.role %></td>
                            <td>
                                <% if (currentUser.id !== u.id) { %>
                                    <a href="/admin/delete/<%= u.id %>" class="action-link delete-link" onclick="return confirm('確定要刪除用戶 <%= u.username.replace(/'/g, '\\\'') %> 嗎？其所有文件也將被刪除。');">刪除用戶</a>
                                    <form action="/admin/reset-password/<%= u.id %>" method="POST" style="display: inline; margin-left: 10px;">
                                        <input type="password" name="newPassword" placeholder="輸入新密碼" required style="padding: 5px; width: 120px; font-size: 0.9em;">
                                        <button type="submit" class="action-link" style="background-color: #ffc107; color: #212529; font-size:0.9em; padding: 6px 10px;" onclick="return confirm('確定要為用戶 <%= u.username.replace(/'/g, '\\\'') %> 重置密碼嗎？');">重置密碼</button>
                                    </form>
                                <% } else { %>
                                    (當前用戶)
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>系統中沒有其他用戶。</p>
        <% } %>
         <p class="note" style="font-size: 0.9em; margin-top: 20px; color: #555;">注意：刪除用戶將同時刪除其所有上傳的文件。</p>
    </div>
</body>
</html>

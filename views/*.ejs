<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登錄 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>登錄</h1>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <form action="/login" method="POST">
            <div>
                <label for="username">用戶名:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div>
                <label for="password">密碼:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">登錄</button>
        </form>
        <p>還沒有帳戶? <a href="/register">註冊</a></p>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>註冊 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>註冊</h1>
        <% if (locals.error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <form action="/register" method="POST">
            <div>
                <label for="username">用戶名:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div>
                <label for="password">密碼:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">註冊</button>
        </form>
        <p>已有帳戶? <a href="/login">登錄</a></p>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的文件 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1><%= user.username %> 的文件</h1>
            <nav>
                <% if (user.role === 'admin') { %>
                    <a href="/admin">管理用戶</a>
                <% } %>
                <a href="/logout">登出</a>
            </nav>
        </header>

        <% if (locals.message) { %>
            <p class="message <%= (locals.message && (message.includes('失敗') || message.includes('错误') || message.includes('沒有選擇文件'))) ? 'error-message' : '' %>"><%= message %></p>
        <% } %>

        <h2>上傳文件</h2>
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <div>
                <label for="userFiles">選擇文件 (可多選):</label>
                <input type="file" id="userFiles" name="userFiles" multiple>
            </div>
            <button type="submit">上傳</button>
        </form>

        <h2>文件列表</h2>
        <% if (files.length > 0) { %>
            <ul>
                <% files.forEach(fileObj => { %>
                    <li>
                        <span><%= fileObj.name %></span>
                        <span class="file-actions">
                            <a href="/download/<%= fileObj.encodedName %>" class="action-link">下載</a>
                            <a href="/delete/<%= fileObj.encodedName %>" class="action-link delete-link" onclick="return confirm('確定要刪除這個文件嗎？ <%= fileObj.name.replace(/'/g, '\\\'') %>');">删除</a>
                        </span>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>您還沒有上傳任何文件。</p>
        <% } %>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理員面板 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>管理員面板 - 用戶管理</h1>
            <nav>
                <a href="/files">返回文件列表</a>
                <a href="/logout">登出</a>
            </nav>
        </header>

        <% if (locals.message) { %>
             <p class="message <%= (locals.message && (message.includes('失敗') || message.includes('错误') || message.includes('不能') || message.includes('無效的用戶ID'))) ? 'error-message' : '' %>"><%= message %></p>
        <% } %>

        <h2>用戶列表</h2>
        <% if (users.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>用戶名</th>
                        <th>角色</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(u => { %>
                        <tr>
                            <td><%= u.id %></td>
                            <td><%= u.username %></td>
                            <td><%= u.role %></td>
                            <td>
                                <% if (currentUser.id !== u.id) { %>
                                    <a href="/admin/delete/<%= u.id %>" class="action-link delete-link" onclick="return confirm('確定要刪除用戶 <%= u.username.replace(/'/g, '\\\'') %> 嗎？其所有文件也將被刪除。');">刪除用戶</a>
                                <% } else { %>
                                    (當前用戶)
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>系統中沒有其他用戶。</p>
        <% } %>
         <p class="note" style="font-size: 0.9em; margin-top: 20px; color: #555;">注意：刪除用戶將同時刪除其所有上傳的文件。</p>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-ষেধ-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>登录 - 网络硬盘</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>登录</h1>
        <% if (error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <form action="/login" method="POST">
            <div>
                <label for="username">用户名:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div>
                <label for="password">密码:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">登录</button>
        </form>
        <p>还没有账户? <a href="/register">注册</a></p>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-ষেধ-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>注册 - 网络硬盘</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <h1>注册</h1>
        <% if (error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>
        <form action="/register" method="POST">
            <div>
                <label for="username">用户名:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div>
                <label for="password">密码:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">注册</button>
        </form>
        <p>已有账户? <a href="/login">登录</a></p>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-ষেধ-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的文件 - 网络硬盘</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1><%= user.username %> 的文件</h1>
            <nav>
                <% if (user.role === 'admin') { %>
                    <a href="/admin">管理用户</a>
                <% } %>
                <a href="/logout">登出</a>
            </nav>
        </header>

        <% if (locals.message) { %>
            <p class="message"><%= message %></p>
        <% } %>

        <h2>上传文件</h2>
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <div>
                <label for="userFiles">选择文件 (可多选):</label>
                <input type="file" id="userFiles" name="userFiles" multiple>
            </div>
            <button type="submit">上传</button>
        </form>

        <h2>文件列表</h2>
        <% if (files.length > 0) { %>
            <ul>
                <% files.forEach(file => { %>
                    <li>
                        <%= file %>
                        <span>
                            <a href="/download/<%= encodeURIComponent(file) %>" class="action-link">下载</a>
                            <a href="/delete/<%= encodeURIComponent(file) %>" class="action-link delete-link" onclick="return confirm('确定要删除这个文件吗？');">删除</a>
                        </span>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>您还没有上传任何文件。</p>
        <% } %>
    </div>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-ষেধ-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>管理员面板 - 网络硬盘</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>管理员面板 - 用户管理</h1>
            <nav>
                <a href="/files">返回文件列表</a>
                <a href="/logout">登出</a>
            </nav>
        </header>

        <% if (locals.message) { %>
            <p class="message"><%= message %></p>
        <% } %>

        <h2>用户列表</h2>
        <% if (users.length > 0) { %>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>用户名</th>
                        <th>角色</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(user => { %>
                        <tr>
                            <td><%= user.id %></td>
                            <td><%= user.username %></td>
                            <td><%= user.role %></td>
                            <td>
                                <% if (currentUser.id !== user.id) { %> <a href="/admin/delete/<%= user.id %>" class="action-link delete-link" onclick="return confirm('确定要删除这个用户吗？其文件不会被删除。');">删除用户</a>
                                <% } else { %>
                                    (当前用户)
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p>系统中没有其他用户。</p>
        <% } %>
         <p style="font-size: 0.8em; margin-top: 20px;">注意：删除用户目前不会删除其上传的文件。</p>
    </div>
</body>
</html>

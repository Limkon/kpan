<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>修改密碼 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
    <link id="theme-stylesheet" rel="stylesheet" href="">
    <style>
        /* 为返回按钮添加一个特定的次要样式 */
        .button-secondary {
            background-color: #6c757d; /* 灰色背景，作为次要按钮 */
            border-color: #6c757d;
        }
        .button-secondary:hover {
            background-color: #5a6268; /* 悬停时颜色变深 */
            border-color: #5a6268;
        }
    </style>
</head>
<body>
    <div class="page-header-container">
         <%- include('partials/theme-switcher') %>
    </div>
    <div class="auth-container">
        <div class="auth-form-container">
            <h1>修改密碼</h1>
            <% if (locals.message) { %>
                 <p class="message <%= (locals.messageType === 'error') ? 'error-message' : '' %>"><%= message %></p>
            <% } %>
            <form action="/change-password" method="POST">
                 <% if (locals.csrfToken) { %>
                     <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                 <% } %>
                 <div class="form-group">
                     <label for="currentPassword">當前密碼:</label>
                     <input type="password" id="currentPassword" name="currentPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="newPassword">新密碼:</label>
                     <input type="password" id="newPassword" name="newPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="confirmNewPassword">確認新密碼:</label>
                     <input type="password" id="confirmNewPassword" name="confirmNewPassword" required>
                 </div>
                 <button type="submit" class="full-width-button">確認修改</button>
            </form>
            
            <%# MODIFIED: 将链接修改为按钮样式 %>
            <div class="auth-switch">
                <a href="/files" class="full-width-button button-secondary" style="text-decoration: none;">返回文件列表</a>
            </div>
        </div>
    </div>
    <script src="/theme.js"></script>
</body>
</html>

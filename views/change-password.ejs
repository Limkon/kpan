<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>修改密碼 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
    <link id="theme-stylesheet" rel="stylesheet" href="">
    <style>
        /*
         * 为“返回”链接创建一个独立的、完整的按钮样式，
         * 以确保它与截图中的“确认修改”按钮视觉上完全一致。
        */
        .custom-button-style {
            /* 按钮形状和尺寸 */
            display: block;
            width: 100%;
            box-sizing: border-box;
            padding: 10px 15px;
            border-radius: 5px;
            border: 1px solid transparent;
            
            /* * MODIFIED: 更新背景颜色以精确匹配截图 
             */
            background-color: #4C8BF5; /* 一个更明亮、更现代的蓝色 */
            color: white;

            /* 文本样式 */
            font-family: inherit;
            font-size: inherit;
            font-weight: 500;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div class="page-header-container">
         <%- include('partials/theme-switcher') %>
    </div>
    <div class="auth-container">
        <div class="auth-form-container">
            <h1>修改密碼</h1>
            <% if (locals.message) { %>
                 <p class="message <%= (locals.messageType === 'error') ? 'error-message' : '' %>"><%= message %></p>
            <% } %>
            <form action="/change-password" method="POST">
                 <% if (locals.csrfToken) { %>
                     <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                 <% } %>
                 <div class="form-group">
                     <label for="currentPassword">當前密碼:</label>
                     <input type="password" id="currentPassword" name="currentPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="newPassword">新密碼:</label>
                     <input type="password" id="newPassword" name="newPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="confirmNewPassword">確認新密碼:</label>
                     <input type="password" id="confirmNewPassword" name="confirmNewPassword" required>
                 </div>
                 <button type="submit" class="full-width-button">確認修改</button>
            </form>
            
            <div class="auth-switch">
                <a href="/files" class="custom-button-style">返回文件列表</a>
            </div>
        </div>
    </div>
    <script src="/theme.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>修改密碼 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
    <link id="theme-stylesheet" rel="stylesheet" href="">
    <style>
        /* 强制重置链接按钮的样式，使其与<button>元素完全一致。
        */
        a.full-width-button {
            /* 模拟按钮的盒模型和显示方式 */
            display: block;
            box-sizing: border-box; 

            /* 统一字体，因为<a>和<button>的默认字体可能不同 */
            font-family: inherit; 
            
            /* 确保文本居中且无下划线 */
            text-align: center;
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="page-header-container">
         <%- include('partials/theme-switcher') %>
    </div>
    <div class="auth-container">
        <div class="auth-form-container">
            <h1>修改密碼</h1>
            <% if (locals.message) { %>
                 <p class="message <%= (locals.messageType === 'error') ? 'error-message' : '' %>"><%= message %></p>
            <% } %>
            <form action="/change-password" method="POST">
                 <% if (locals.csrfToken) { %>
                     <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                 <% } %>
                 <div class="form-group">
                     <label for="currentPassword">當前密碼:</label>
                     <input type="password" id="currentPassword" name="currentPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="newPassword">新密碼:</label>
                     <input type="password" id="newPassword" name="newPassword" required>
                 </div>
                 <div class="form-group">
                     <label for="confirmNewPassword">確認新密碼:</label>
                     <input type="password" id="confirmNewPassword" name="confirmNewPassword" required>
                 </div>
                 <button type="submit" class="full-width-button">確認修改</button>
            </form>
            
            <div class="auth-switch">
                 <%# 清除了内联样式，因为样式已在<head>中定义 %>
                <a href="/files" class="full-width-button">返回文件列表</a>
            </div>
        </div>
    </div>
    <script src="/theme.js"></script>
</body>
</html>

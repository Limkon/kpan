<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>錯誤 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
    <link id="theme-stylesheet" rel="stylesheet" href=""> </head>
<body>
    <div class="auth-container">
        <div class="auth-form-container" style="max-width: 500px;">
            <h1>發生錯誤</h1>
            <div class="error-message" style="margin-bottom: 20px; text-align: left;">
                <p><%= message %></p>
            </div>
            <p style="text-align: center;">
                <% if (locals.user && locals.user.username) { %>
                    <a href="/files" class="button-link">返回我的文件</a>
                <% } else { %>
                    <a href="/login" class="button-link">前往登錄頁面</a>
                <% } %>
            </p>
        </div>
    </div>
    <%- include('partials/theme-switcher') %>
    <script src="/theme.js"></script>
</body>
</html>

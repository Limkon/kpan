<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>錯誤 - 網路硬碟</title>
    <link rel="stylesheet" href="/style.css">
    <link id="theme-stylesheet" rel="stylesheet" href="">
    <style>
        .error-page-container {
            text-align: center;
            padding-top: 50px;
        }
        .error-page-container h1 {
            font-size: 2em;
            margin-bottom: 20px;
            color: #dc3545; /* Bootstrap danger color */
        }
        .error-page-container p {
            font-size: 1.2em;
            margin-bottom: 30px;
        }
        .error-page-container .home-link {
            display: inline-block;
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-weight: 500;
        }
        .error-page-container .home-link:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <div class="page-header-container">
        <%- include('partials/theme-switcher') %>
    </div>

    <div class="container error-page-container">
        <h1>發生錯誤</h1>
        <% if (locals.message) { %>
            <p><%= message %></p>
        <% } else { %>
            <p>抱歉，發生了一個未知的錯誤。</p>
        <% } %>
        
        <% if (locals.user) { %>
            <p><a href="/files" class="home-link">返回我的文件</a></p>
        <% } else { %>
            <p><a href="/login" class="home-link">前往登錄頁面</a></p>
        <% } %>
    </div>
    <script src="/theme.js"></script>
</body>
</html>
